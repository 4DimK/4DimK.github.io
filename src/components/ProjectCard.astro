---
const { title, image, index } = Astro.props;
const isLeft = index % 2 === 0;
---

<div class={`flex flex-col md:flex-row items-center my-12 ${isLeft ? "" : "md:flex-row-reverse"}`}>
  <img
    src={image}
    alt={title}
    class="w-full md:w-1/2 h-64 object-cover rounded-lg shadow-lg"
  />

  <div class="md:w-1/2 p-6 text-center md:text-left">
    <h2 class="text-2xl font-bold mb-4">{title}</h2>
    <p class="text-gray-600">Project description goes here.</p>
  </div>
</div>
